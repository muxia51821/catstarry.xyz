当前待办事项0705  re:0707
结合以下文件的基础定位，阅读以下文档并回答相关问题：

- `docs/DASHBOARD.md` — 进度看板
  - `docs/workflow-orchestration.md` — 执行手册
  - `CONTEXT.md` — 性质标签 + 开发状态
  - `AGENTS.md` — 全局约束

D:\catstarry.xyz\AGENTS.md 1.核对 AGENTS.md 中的技术栈描述是否与 architecture.md 一致。如果不一致，仅以 architecture.md 为准更新 AGENTS.md。还要详见docs/tech-decisions-20260703.md和 readme.md 这句话是不是需要修改？
2.# Rule Precedence 的优先级链条（AGENTS.md > workflow-orchestration > CONTEXT.md > labels > local heuristics）中，是否需要加入即将产出的 DESIGN.md 或 docs/agents/frontend-rules.md？如果需要，应该排在什么位置？

·A 组：AGENTS.md
A1. 技术栈描述是否与 architecture.md 一致
当前 AGENTS.md 技术栈行：
Astro hybrid + React (shadcn/ui) + CF Workers + D1 + KV + R2。详见 README.md 和 docs/tech-decisions-20260703.md。

architecture.md 技术栈映射表加了 2 个 D1（catstarry-db + finance-db）+ 3 个 KV namespace + R2 bucket 名称。但 AGENTS.md 的技术栈行是给 Agent 看的全局概述，不需要列到 namespace 级别。
结论：不需要改。AGENTS.md 的概述是正确的。但 详见 README.md 和 docs/tech-decisions-20260703.md 可以改为 详见 docs/architecture.md——因为 tech-decisions 是 Phase 0 预判，architecture 才是权威。
A2. Rule Precedence 是否加入 DESIGN.md / frontend-rules.md
当前链条：AGENTS.md > workflow-orchestration > CONTEXT.md > labels > local heuristics
DESIGN.md：是视觉设计规范，不是流程控制文件。它约束的是 Agent 写 UI 时的视觉行为，不和 workflow-orchestration 或 CONTEXT.md 冲突。不需要进优先级链——它的约束力来自 workflow-orchestration 的 Phase 4/5 步骤引用，不是自身的"优先级"。
frontend-rules.md：是 Phase 5.0 固化的开发规则，属于 local heuristics 的一种（项目本地约定）。不需要单独进优先级链——已有的 local heuristics 已覆盖。
结论：Rule Precedence 链条不变。

D:\catstarry.xyz\CONTEXT.md
1.“CONTEXT.md 的【领域术语】部分列举了多个术语解释，这与 GLOSSARY.md 内容重复。为避免信息冗余，是否将其精简为一句‘见 GLOSSARY.md’？” 
2.【设计基调】部分当前标注为 [原型约定 | Phase 4 重新裁决]。考虑到我们即将在 Phase 4 引入 DESIGN.md，此部分是否需要现在更新说明（例如补充‘Phase 4 将借鉴 awesome-design-md 结构生成专属 DESIGN.md’），还是保持原样等 Phase 4 执行时再改？” 
3.【目录结构】部分是否需要预先补充 Phase 4 即将新增的文件路径（如根目录的 DESIGN.md、docs/design/notes.md）？
4./login 的登录鉴权在哪里？ cold-start-governance.md说看context.md但是这里没有。
这个鉴权方案应该是docs\architecture\auth.md，请复核。
5.请检查 CONTEXT.md 中对架构的描述是否与最新的 docs/architecture.md 保持同步


·B 组：CONTEXT.md
B1. 领域术语重复 GLOSSARY.md
CONTEXT.md 列出了 7 个核心术语（木下、cati、碎碎念、剪藏、备忘录感、PE 温度计、三级熔断），GLOSSARY.md 有同样的条目。但两者的角色不同——CONTEXT.md 是给 Agent 读的「快速上下文」，GLOSSARY.md 是权威术语表。保留 CONTEXT.md 里 7 个高频术语做快速参考是合理的（Agent 不用跳文件），但需要加一句指向：完整术语见 GLOSSARY.md。
结论：精简为保留 7 个核心术语 + 尾部加 完整术语见 GLOSSARY.md。
B2. 设计基调标注
当前：[原型约定 | Phase 4 重新裁决]。这已经很准确了。Phase 4 还没执行，不要预先改——Phase 4 agent 应该读到这个标记后自行决定怎么处理。加一句「Phase 4 将借鉴 awesome-design-md + taste-skill」是帮 Phase 4 agent 做准备，但这是 phase4-briefing.md 的事，不是 CONTEXT.md 的事。
结论：保持原样，不改。
B3. 目录结构是否预加新文件路径
CONTEXT.md 目录结构现在标着 [已锁定]（Phase 3 锁定）。新增 DESIGN.md 和 docs/design/notes.md 不是架构层面的目录结构变更——是 Phase 4 的产出物路径。应该由 Phase 4 agent 完成后更新 CONTEXT.md。
结论：不改。Phase 4 完成后更新。
B4. /login 鉴权在哪里
CONTEXT.md 鉴权信息缺失。cold-start 指向 docs/architecture/auth.md（正确），但 CONTEXT.md 的后端约定只提了 /api/auth 路由扩展，没提 /login 的鉴权流程。需要补。
结论：需要在 CONTEXT.md 后端约定或技术架构节里加一句鉴权说明，引用 docs/architecture/auth.md。
B5. CONTEXT.md 架构描述是否与 architecture.md 同步

D:\catstarry.xyz\docs\SITEMAP.md
哪些页面“允许 SSR依赖 D1”，哪些必须“退化为 SSG + hydration”?请对照 docs/architecture.md 中的数据流向，在 SITEMAP.md 中明确标出：哪些页面是 SSR 且依赖 D1/API 实时数据，哪些页面是 SSG 纯静态？

添加| 内容 | 碎碎念 + 剪藏混合时间线，单列垂直滚动，SSR 依赖 D1 feed_posts 表 |


D:\catstarry.xyz\docs\cold-start-governance.md 1.【项目文件地图】中是否需要新增根目录的 DESIGN.md 以及 docs/design/ 目录的说明？2.【各 Phase 产出】列表是否尚未更新 Phase 4、5、6 的内容？如果是，请根据 workflow-orchestration.md 补全。
已更新
关于taste-skill：a.外部 skill（如 taste-skill、awesome-design-md）在当前环境中无法通过斜杠命令直接调用。请将它们的核心规则文本作为系统 Prompt 的一部分注入，或者在对应 Phase 的步骤说明中，明确要求 AI 读取本地化的规则文件（例如 docs/agents/taste-skill-core.md）。
b.请基于我们讨论的最终方案，直接修改 workflow-orchestration.md 的 Phase 4 和 Phase 5 表格，明确写出 design-an-interface、prototype、web-design-engineer 与 taste-skill 规则的串联执行顺序。
