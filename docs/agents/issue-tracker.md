# Issue Tracker

- **Type**: Local markdown
- **Location**: `.scratch/<feature>/` — each feature request and bug gets its own subdirectory
- **Convention**: Each issue is a markdown file (e.g. `.scratch/add-dark-mode/issue.md`). Supporting files (screenshots, notes) live alongside it.
- **External PRs as triage surface**: No — local markdown has no PRs.
